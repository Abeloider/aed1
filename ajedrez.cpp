#include <iostream>

using namespace std;

int main () {
    int casos;

    // Numero de casos
    cout << "Pon el numero de casos que quieres: ";
    cin >> casos;
    int i=0;
    int n=0;
    int x1=0; // eje x1
    int y1=0; // eje y1
    int x2=0; // eje x2
    int y2=0; // eje y2
        while (n<4){
            cin >> x1 >> y1 >> x2 >> y2;
            cout << x1 << " " << y1 << " " << x2 << " " << y2  ;
            n++;
        }

    int ejex=0;
    int ejey=0;
    if ((x1+y1)%2==0){   // si es par verde
        if (((x1+y2)%2==0)==((x2+y2)%2==0)){
        while (x)
        }
    }
        if ((x2+y2)%2!=0) { // si es impar blanco

    }
    else (((x1+y2)%2==0)!=((x2+y2)%2==0)) {
        cout << "no move"
    }


}
